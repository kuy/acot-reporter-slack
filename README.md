# acot-reporter-slack

A Slack reporter for [acot](https://github.com/acot-a11y/acot).

## Installation

Install via npm:

```bash
$ npm install --save acot-reporter-slack
```

## Usage

```javascript
module.exports = {
  reporter: "slack",
};
```
